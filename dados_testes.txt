Credenciais:

Usuário	Email	Senha
Admin	admin@loja.com	12345678
Funcionário	funcionario@loja.com	12345678
Cliente	cliente@loja.com	12345678


 Acessos:
Aplicação: http://localhost:8080

phpMyAdmin: http://localhost:8081 (usuário: root, senha: root123)

Admin (se criado): admin@smcomponentes.com / password123

cliente@loja.com cliente123

Admin: admin@smcomponentes.com / admin123

Usuário: usuario@smcomponentes.com / usuario123

 Acessar phpMyAdmin
Abra no navegador: http://localhost:8081

Credenciais:

Servidor: mysql (ou smcomponentes_db)

Usuário: root

Senha: root123

admin@teste.com', 'password' => 12345678'




# Primeira vez ou quando precisar configurar tudo
./setup.sh

# Reconstruir do zero (limpa tudo)
./rebuild.sh

# Iniciar containers (MySQL + phpMyAdmin)
./start.sh

# Parar containers
./stop.sh

# Verificar status
./status.sh


# Comando completo
./vendor/bin/phpunit --testsuite=Unit --testdox --no-output 2>&1 | \
  grep -v "Warning" | \
  grep -v "Ambiguous" | \
  grep -v "Skipping" | \
  grep -v "Runtime:" | \
  grep -v "Configuration:" | \
  grep -v "✅ Coluna" | \
  grep -v "Deprecations" | \
  grep -v "PHPUnit" | \
  grep -v "OK, but there were issues"

# Alias (adicionar ao ~/.bashrc)
alias test-unit="vendor/bin/phpunit --testsuite=Unit --testdox --no-output 2>&1 | grep -v \"Warning\" | grep -v \"Ambiguous\" | grep -v \"Skipping\" | grep -v \"Runtime:\" | grep -v \"Configuration:\" | grep -v \"✅ Coluna\" | grep -v \"Deprecations\" | grep -v \"PHPUnit\" | grep -v \"OK, but there were issues\""

# Executar
test-unit